{
  "name": "tokenisation",
  "version": "0.0.1",
  "description": "A project to generate the sequence diagrams explaining the tokenisation flows",
  "main": "build.js",
  "scripts": {
    "build": "node build.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "SKS",
  "license": "ISC",
  "dependencies": {
    "mermaid.cli": "^0.5.1"
  }
}
